I",(function() {
  var extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  DemoApp.Routers.Accounts = (function(superClass) {
    extend(Accounts, superClass);

    function Accounts() {
      return Accounts.__super__.constructor.apply(this, arguments);
    }

    Accounts.prototype.routes = {
      'pages/accounts': 'allAccounts',
      'pages/transactions': 'allTransactions'
    };

    Accounts.prototype.initialize = function() {
      this.collection = new DemoApp.Collections.Accounts();
      this.collection.reset($('#app').data('accounts'));
      this.currency = new DemoApp.Collections.Currencies();
      this.currency.fetch({
        reset: true
      });
      this.transactions = new DemoApp.Collections.Transactions();
      return this.transactions.fetch({
        reset: true
      });
    };

    Accounts.prototype.allAccounts = function() {
      var view;
      view = new DemoApp.Views.AccountsIndex({
        collection: this.collection
      });
      return $('#app').html(view.render().el);
    };

    Accounts.prototype.allTransactions = function() {
      var view;
      view = new DemoApp.Views.TransactionsIndex({
        collection: this.transactions
      });
      return $('#app').html(view.render().el);
    };

    return Accounts;

  })(Backbone.Router);

}).call(this);
:ET